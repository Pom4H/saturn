/** Canonical authored source used by the landing. Original equipment, no alternate renderer. */
export const landingProjectSource = "import { pipe, project, simulation, system } from '@saturn/core';\n\nconst water = system('water', 'Насосный контур');\n\nconst pump = simulation('P-01', 'pump', {\n  system: 'water',\n  at: { x: 390, y: 190 },\n});\nconst tank = simulation('TK-01', 'reservoir', {\n  system: 'water',\n  at: { x: 60, y: 330 },\n  inputs: { inflow: pump.flow, demand: pump.flow },\n  parameters: { initialLevel: .3 },\n});\n\nconst suction = pipe('suction', tank.ports.outlet, pump.ports.inlet);\nconst delivery = pipe('delivery', pump.ports.outlet, tank.ports.inlet);\n\nexport default project('landing-pump', {\n  title: 'Насосный контур',\n  description: 'Учебная симуляция. Без подключения к оборудованию.',\n  systems: [water], simulations: [tank, pump],\n  connections: [suction, delivery],\n  signals: [], alarms: [], reports: [],\n});\n";
export const landingProjectFiles = (source = landingProjectSource): Record<string, string> => ({ 'plant.ts': source });
