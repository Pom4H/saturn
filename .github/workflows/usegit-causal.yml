# usegit:managed
name: usegit

on:
  push:
  pull_request:

jobs:
  causal-history:
    permissions:
      contents: write
    uses: Pom4H/usegit/.github/workflows/reusable.yml@main
    with:
      tool_ref: main
