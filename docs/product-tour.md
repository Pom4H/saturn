# Product tour

The self-hosted product-tour workflow boots a clean Saturn server and records one browser session across engineer, operator and viewer roles. The run fails if role boundaries are violated. The generated WebM is uploaded as the `saturn-product-tour` workflow artifact.
