# First pump

This is a normal Saturn project directory.

```sh
saturn check examples/first-pump
saturn open examples/first-pump
```

There is no Saturn manifest. `package.json` provides project/package identity and `src/plant.ts` is the conventional entrypoint. Git is ordinary source history; runtime receives a content-addressed build artifact.
