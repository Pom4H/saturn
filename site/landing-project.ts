/** Canonical authored source used by the landing. No legacy scene DSL. */
export const landingProjectSource = "import { cable, pipe, project, simulation, system } from '@saturn/core';\n\nconst water = system('water', 'Вода');\nconst power = system('power', 'Электроснабжение');\n\nconst tank = simulation('TK-01', 'reservoir', {\n  system: 'water',\n  at: { x: 60, y: 170 },\n  inputs: { inflow: .2, demand: .2 },\n});\nconst pump = simulation('P-01', 'pump', {\n  system: 'water',\n  at: { x: 390, y: 190 },\n});\nconst generator = simulation('GEN-01', 'alternator', {\n  system: 'power',\n  at: { x: 60, y: 430 },\n  inputs: { rpm: 1500, load: .5 },\n});\nconst drive = simulation('M-01', 'electric-motor', {\n  system: 'power',\n  at: { x: 390, y: 430 },\n  inputs: { voltage: generator.voltage, demand: .85 },\n});\n\nconst suction = pipe('suction', tank.ports.outlet, pump.ports.inlet);\nconst feeder = cable('feeder', generator.ports.output, drive.ports.power, { medium: 'power' });\n\nexport default project('landing-pump', {\n  title: 'Насосная станция',\n  description: 'Один TypeScript-проект: оборудование, трубы и кабели.',\n  systems: [water, power],\n  simulations: [tank, pump, generator, drive],\n  connections: [suction, feeder],\n  signals: [],\n  alarms: [],\n  reports: [],\n});\n";
export const landingProjectFiles = (source = landingProjectSource): Record<string, string> => ({ 'plant.ts': source });
